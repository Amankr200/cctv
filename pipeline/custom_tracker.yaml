tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 150  # Increased from 30 to handle occlusion (keeps lost tracks in memory for ~5 seconds)
match_thresh: 0.8
